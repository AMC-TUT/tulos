<% if @model.errors.any? %>
   <div class="alert alert-danger">
      <button type="button" class="close" data-dismiss="alert">&times;</button>
      <p><strong><%= pluralize(@model.errors.count, "error") %> prohibited this item from being saved:</strong></p>
      <ul>
         <% @model.errors.full_messages.each do |msg| %>
         <li><%= msg %></li>
         <% end %>
      </ul>
   </div>
<% end %>